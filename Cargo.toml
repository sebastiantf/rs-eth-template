[package]
edition = "2021"
name = "rs-eth-template"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
ethers = {version = "0.17.0", features = ["ws", "rustls"]}
